#include "CLBuffer.hpp"

namespace MATD {
	namespace DTYPES {
		namespace OPENCL {
			template<typename T>
			void Buffer<T>::Bind(const WorkItem* workItem, size_t index)
			{
			}

			template<typename T>
			void Buffer<T>::Delete()
			{
			}
		}
	}
}