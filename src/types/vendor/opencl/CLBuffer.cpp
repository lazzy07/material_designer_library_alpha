#include "CLBuffer.hpp"

namespace MATD {
	namespace DTYPES {
		namespace OPENCL {
			template<typename T>
			void Buffer<T>::Bind(const ENGINE::WorkItem* workItem, size_t index)
			{
			}

			template<typename T>
			void Buffer<T>::Delete()
			{
			}
		}
	}
}