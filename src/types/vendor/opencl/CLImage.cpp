#include "CLImage.hpp"

namespace MATD {
	namespace DTYPES {
		namespace OPENCL {
			template<typename T>
			void Image<T>::Bind(const ENGINE::WorkItem* workItem, size_t index)
			{
			}

			template<typename T>
			void Image<T>::Delete()
			{
			}
		}
	}
}